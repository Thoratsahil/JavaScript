//prompt the user to enter their full name,generate a username for them based on input  start username with  @ ,followed by their  full name and ending  with the  fullname with length

let st=prompt("Enter your Name");
//console.log(st);

let ct="@" + st + st.length;
console.log(ct);